aio-pika==9.6.2
aiormq==6.9.4
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
certifi==2026.6.17
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.2
cryptography==49.0.0
distro==1.9.0
fastapi==0.139.0
fpdf2==2.8.7
google-auth==2.55.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jsonpatch==1.33
jsonpointer==3.1.1
langchain-text-splitters==1.1.2
langsmith==0.9.7
mariadb==1.1.14
multidict==6.7.1
openai>=2.0.0,<3.0.0
orjson==3.11.9
packaging==25.0
pamqp==3.3.0
passlib==1.7.4
propcache==0.5.2
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
PyJWT==2.13.0
python-multipart==0.0.32
PyYAML==6.0.2
python-docx==1.2.0
regex==2026.6.28
requests==2.34.2
requests-toolbelt==1.0.0
rsa==4.9.1
sniffio==1.3.1
SQLAlchemy==2.0.51
starlette==1.3.1
tenacity>=8.2.3
tiktoken==0.13.0
tqdm==4.68.3
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uuid_utils==0.16.2
uvicorn==0.49.0
xxhash==3.8.0

# ── PaddleOCR pipeline (blanc.core.ocr) ──────────────────────────────────
# Used by document_analysis.image_to_mermaid for images > 2000x2000 px.
# Heavy install (~1-2 GB with native wheels) — pin loosely so we can
# adopt PP-OCRv6 minor bumps without a code change.
#
# opencv-python-headless: pinned to 4.x. PaddleOCR still targets the
# OpenCV 4 ABI; 5.x wheels (released Oct 2025) break at import.
paddleocr>=3.7.0
paddlepaddle>=3.0.0
opencv-python-headless>=4.10.0,<5.0.0
numpy>=1.26.0
Pillow>=10.0.0
yarl==1.24.2
zstandard==0.25.0
PyMuPDF==1.28.0
PyMuPDFb==1.24.10
colorlog==6.10.1
instructor>=1.3.0

# ─── Optional LLM provider extras ────────────────────────────────────────────
# The core LLMClient is vendor-neutral. Install the SDK(s) for the
# provider(s) you want to use. When migrating to pyproject.toml, these
# belong under [project.optional-dependencies]:
#
#   openai    = ["openai>=2.0.0,<3.0.0", "instructor>=1.3.0"]     # default
#   anthropic = ["anthropic>=0.34", "instructor>=1.3.0"]
#   litellm   = ["litellm>=1.50"]
#
# Uncomment to enable LiteLLM (multi-vendor routing):
# litellm>=1.50

# ─── RAG backend ─────────────────────────────────────────────────────────────
# Local RAG store. Small pure-Python package; safe to install even for
# users who set rag_config.backend to "http".
chromadb>=0.5.0

# Embedding provider for the local RAG backend. Optional — commented out
# because it pulls in torch (~2 GB). Install with `pip install
# sentence-transformers` when using rag_config.embedder.provider =
# "sentence_transformers". Not needed when using "openai".
# sentence-transformers>=2.7.0

# ─── Optional storage backend extras ─────────────────────────────────────────
# The storage system is plugin-based. Built-ins: local (no extra),
# s3 (boto3 — optional). Add your own via the "blanc.storage_backends" entry point.
#
# Under pyproject.toml:
#   s3  = ["boto3>=1.34"]
#   gcs = ["google-cloud-storage>=2.16"]   # if you add a GCS plugin
#
# Uncomment to enable the built-in S3 / S3-compatible backend (AWS, MinIO,
# Cloudflare R2, Wasabi, Backblaze B2, ...):
# boto3>=1.34
